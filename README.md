# fancy
fancy


```sh
go get -u all
go mod tidy
git tag                                 #check current version
git tag v0.0.01                          #set tag version
git push origin --tags                  #push tag version to repo
go list -m github.com/Fancyyy21/FancyBackendGISBaru@v0.0.01   #publish to pkg dev, replace ORG/URL with your repo URL
```